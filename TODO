- Document the source code better.

- Add support for NS names and IPs to pgsqldns-conf.

- Add support for wildcard DNS records by modifying sql_record_select to
  search for shorter and shorter pieces of the domain name prefixed with
  a "*" until something matches.
