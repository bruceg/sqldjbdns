- Move ALL SQL commands and query functions into seperate module (started).

- Escape SQL special characters in stralloc_cat_dns_to_sql and
  stralloc_catb_dns_to_sql to prevent arbitrary SQL command attacks.

- Put proper values in for the "hostmaster" string in SOA responses.

- Parse name and IP TTL values for the nameservers.

- Start all address lists (nameservers, etc.) at a random offset.

- Produce documentation pages.

- Document the source code better.

- Build a sqldns-conf program.

- Implement a time-to-die specification, dynamically decreasing TTL
  values until the given time is reached.
